{
  "name": "pi-jdtls",
  "version": "1.0.0",
  "description": "Eclipse JDT Language Server bridge for pi",
  "keywords": ["pi-package"],
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "sync": "mkdir -p vendor && cp ../shared/tool-activation.ts vendor/",
    "pretest": "npm run sync",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepack": "npm run sync"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.7"
  },
  "pi": {
    "extensions": ["."]
  }
}
