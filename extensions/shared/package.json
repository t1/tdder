{
  "name": "pi-shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript modules vendored into pi extensions at sync time",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "pretest": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^7.0.0",
    "vitest": "^5.0.0"
  }
}
